---
description: 每次实验完成后必须更新统一结果文档
alwaysApply: true
---

# 实验完成后更新结果文档

## 核心规则

**每次算法实验运行完成后，必须将结果写入 `runs/RESULTS.md`。**

这是平台级的横向比较文档，所有算法、所有市场的实验结果在此统一记录，便于跨算法比较。

## 具体做法

### 1. 实验完成后立即更新

当一个算法在任何市场完成实验（包括新算法、参数调优、重跑），必须：

1. 读取 `runs/predictions/<market>/<algorithm>/metrics.json` 中的指标
2. 更新 `runs/RESULTS.md` 中对应的表格行
3. 如果是新算法，在文档中新增一个"算法详情"小节
4. 更新"更新日志"表格，注明本次变更

### 2. 横向比较表必须保持最新

`RESULTS.md` 的 §2 "算法横向比较" 中的 MAE/RMSE 对比表必须反映最新结果：
- 每个市场每个算法一列
- 标注最优值（加粗）和相对改进百分比
- 如果某个指标某算法未输出，标注"—"

### 3. 文档结构

```
runs/RESULTS.md
├── §1 测试集划分（统一）     ← 来自 config/markets/*.yaml
├── §2 算法横向比较           ← MAE/RMSE/Corr 对比表
├── §3 各算法详情             ← 每个算法一小节
├── §4 分析与备注             ← 定性分析
└── §5 更新日志               ← 每次更新追加一行
```

### 4. 新增算法时

- 在 §2 的对比表中增加一列
- 在 §3 中新增一个算法详情小节（代码路径、方法简述、关键参数、各市场指标表）
- 在 §4 分析中补充与已有算法的对比解读

## 反例

```
# ❌ BAD：跑完实验只看终端输出，不更新文档
python algorithms/new_algo/run.py --market all
→ "MAE=75.3，不错" → 结束

# ✅ GOOD：跑完实验后更新 RESULTS.md
python algorithms/new_algo/run.py --market all
→ 读取 metrics.json
→ 更新 runs/RESULTS.md（对比表 + 算法详情 + 更新日志）
→ 告知用户结果
```
